# PlatformIO build artifacts
.pio/

# clangd / LSP (generated; absolute paths — regenerate with `pio run -t compiledb`)
compile_commands.json
.cache/

# Doxygen-generated HTML (regenerate with `doxygen Doxyfile`)
.doxygen/

# Secrets (real WiFi creds live here, never committed) — arrives in Plan 2
include/secrets.h

# macOS
.DS_Store

# Device firmware dumps / backups (large, device-specific binaries)
*.bin
